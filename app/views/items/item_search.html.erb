<%= render "shared/header" %>
<div class="hashtag_names">キーワード<span class="color"><<%=@keyword%>></span> の商品一覧</div>

<div class="contents-row">
<%unless @items == nil %>
  <% @items.each do |item| %>
    
    <li class='llist'>
      <%= link_to item_path(item.id) do %>
        <div class='item-img-content'>
          <%= image_tag item.image, class: "item-img" %>
          <% unless item.order == nil %>
            <%# 商品が売れていればsold outの表示 %>
            <div class='sold--out'>
            <span>Sold Out!!</span>
            </div>
            <%# //商品が売れていればsold outの表示 %>
          <% end %>
        </div>
        <div class='item-info'>
          <h3 class='item-name'>
            <%= item.name %>
          </h3>
          <div class='item--price'>
            <span><%= item.price %>円<br>(税込み)</span>
            <div class='star--btn'>
              <%= image_tag "star.png", class:"star-icon" %>
              <span class='star-count'>0</span>
            </div>
          </div>
        </div>
      <% end %>
    </li>
  <% end %>
<%if @items.length == 0 %>
<div class="no_hits"><p>検索条件に当たる商品はありませんでした</p></div>
<%end%>
<%end%>

</div>
